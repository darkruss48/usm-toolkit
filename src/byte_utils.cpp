#include "byte_utils.hpp"

// All implementations are inline in the header file
// This source file exists for build system compatibility
// and potential future additions that require non-inline implementation

namespace usm_toolkit {

// Reserved for future non-inline implementations
// I may not do it but we never know

} // namespace usm_toolkit
